using FluentAssertions;
using NUnit.Framework;

namespace Day2.Tests
{
    public class Tests
    {
        //[TestCase("up 3", "up", 3)]
        //[TestCase("down 5", "down", 5)]
        //public void Test1(string input, string expectedDirection, int expectedMagnitude)
        //{
        //    var command = new Command(input);

        //    command.Direction.Should().Be("up");
        //    command.Magnitude.Should().Be(3);
        //}
    }
}